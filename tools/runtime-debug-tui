#!/usr/bin/env python3
"""runtime-debug-tui — thin textual shell over the same Debug Query Core.

Requires textual: UV_CACHE_DIR=.uv-cache uv run --with textual python -m runtime_debug.tui.app
"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "runtime_debug"))

from runtime_debug.tui.app import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
