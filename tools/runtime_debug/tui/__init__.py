"""tui package — view models (stdlib) and the textual shell (framework-imported
only in app.py). Keep view_models.py free of framework imports."""

from .view_models import diagnosis_view, filter_state, open_run, tree_view  # noqa: F401