# Evidence Index — runtime-iterative-full-traversal-acceptance

| File | Content |
|---|---|
| `A-human-gate-and-baseline.md` | Human approval, artifact digests, owners, scope, pre-existing tree state, asset layout decision |
| `runtime-baseline-manifest.sha256` | SHA-256 manifest of all Runtime production files at implementation start (216 files) |
| `semantic-ir-records.md` | Development Semantic IR per task group (DesiredReality…SemanticResolution) |
| `G0-real-emulator-prerequisites.md` | Real emulator + real Settings anchor verification (pre-Stage-A) |
| `B-campaign-runner.md` | Campaign runner acceptance evidence (added on completion) |
| `C-knowledge-fixture.md` | Knowledge fixture acceptance evidence |
| `D-fixture-persistence.md` | Persistence acceptance evidence |
| `E-plandelta-recorder.md` | PlanDelta recorder acceptance evidence |
| `F-settings-binding.md` | Settings binding acceptance evidence |
| `G-stage-a-conservative-campaign/` | Stage A campaign rounds, run evidence, fixture v1 increment |
| `H-stage-b-online-adaptation/` | ≥3 adaptation rounds with PlanDelta provenance |
| `I-stage-c-persisted-reuse/` | Frozen fixture reuse campaign evidence |
| `J-phase26a-acceptance.md` | PHASE26A_ACCEPTANCE_RESULT |
| `K-stage-d-full-traversal/` | 2.6B full traversal evidence |
| `L-advisory-package.md` | Simulator-derived Advisory Knowledge Package |
| `M-full-regression.md` | Final regression numbers |
| `SOURCE-NORMALIZATION-ANCHOR-CONFIRMATION-DIAGNOSTIC-RESULT.md` | Post-`FRAME_LOCAL_COMPOSITION_VALIDITY_VETO_REPAIR_GATE` normalization blocker diagnosis (DIAGNOSIS ONLY / Phase 2.6 STOPPED) |
| `SOURCE-NORMALIZATION-ANCHOR-CONFIRMATION-REPAIR-RESULT.md` | `SOURCE_NORMALIZATION_ANCHOR_ADJACENT_CONFIRMATION_REPAIR_GATE` repair result: anchor-adjacent exact confirmation resolves without growing union; falsifiers fail-closed; Phase 2.6 STOPPED |
| `SEMANTIC-PROJECTION-BOUNDS-DIAGNOSTIC-RESULT.md` | `PROJECT_LEADER_SEMANTIC_PROJECTION_BOUNDS_DIAGNOSTIC_GATE` result: bounds exception is float32-subtraction reconstruction overflow (ROUNDING_BOUNDARY_BUG) in `SemanticObservationFactProjector.Normalize`, single occurrence drops whole frame (seq24/25); production repair NOT authorized; Phase 2.6 STOPPED |
| `SEMANTIC-PROJECTION-BOUNDS-REPAIR-RESULT.md` | `PROJECT_LEADER_SEMANTIC_PROJECTION_BOUNDS_REPAIR_GATE` result: minimal repair (widen to double before subtraction in `Normalize`) RED→GREEN 11/11, illegal-bounds fail-closed preserved, full suites green, fresh real run-1: zero projection exceptions / zero whole-frame DROP / root resolved 16 sources; residual blocker recorded (normalizer pair 1 seq22→seq25 non-monotonic anchors, element-order reorder), FaultContainmentPressure registered PRESENT, Phase 2.6 STOPPED |
| `SOURCE-NORMALIZER-LOGICAL-ORDER-DIAGNOSTIC-RESULT.md` | `PROJECT_LEADER_SOURCE_NORMALIZER_ORDER_SENSITIVITY_DIAGNOSTIC_GATE` result: pair 1 (seq22→seq25) classified **A. REPRESENTATION_ORDER_DRIFT** — logical top-to-bottom order unchanged (12 shared rows, identical spatial ranks, constant ΔCenterY≈−0.146 / 350px scroll), only perception serialization reordered `row_010` toolbar (partial-width idx13/pos3 → full-width idx0/pos0); anchor mapping `[3,0,1,2,4,5,6,9,10,11,12,13,14]` non-monotonic → fail-closed; SameSource for row_010 proven (StableKey+text+spatial rank); invariant intent = logical/spatial forward order, operand = array order; minimal repair candidate = CenterY projection + same-StableKey row-group adjacency (ORDER-ONLY, verified monotonic `[0,1,2,3,4,5,8,9,10,11,12,13,14]`, 4 insertions, zero guard/completeness change); next gate `SOURCE_NORMALIZER_LOGICAL_ORDER_RECONCILIATION_REPAIR_GATE`; Phase 2.6 STOPPED |
| `SOURCE-NORMALIZER-LOGICAL-ORDER-REPAIR-RESULT.md` | `PROJECT_LEADER_SOURCE_NORMALIZER_LOGICAL_ORDER_RECONCILIATION_REPAIR_GATE` result: minimal ORDER-ONLY repair implemented — logical-order projection (StableKey row grouping + CenterY band, elementIndex tie-break) + boundary-plausibility gate (spatial-edge bands; prevents mid-page rows surfaced at the projected head from being boundary-truncated — the fact that broke the quiescence S3/S11 controls pre-gate). RED→GREEN: 3 discriminators RED (captured seq22→25 falsifier Unresolved, permutation property, same-text) → 13/13 GREEN (pair 1 resolves, 4 new sources, exact 19-signature union); 8 counterexample classes + permutation property + 2 boundary-gate regressions all preserved; full suite 2315 passed / 10 env-only failures (attribution: identical without the change); check-consistency ALL PASS; fresh real round-1: zero `Source normalization is unresolved` anywhere in trace (previous blocker terminal gone), new first blocker recorded = root Unknown affordances (`LOu` garbled band seq7/8, `Safety & emergency` seq13/14 — perception/semantic coverage, non-normalizer Owner); Phase 2.6 STOPPED |
| `UNKNOWN-AFFORDANCE-FRAGMENT-COMPLETENESS-BLOCKER-DIAGNOSTIC-RESULT.md` | Revealed-next-blocker diagnostic (evidence-first, human-phone-view, then code): terminal `Unknown interaction affordances remain` = phantom obligations, NOT page-level unknowns — (1) text-less row-background container fragments (6..13 per accepted child frame; structurally unjudgeable by the text-centric semantics patterns AND unresolvable by the completeness Unknown-bypass which needs StableKey/prior-run evidence), (2) `Safety & emergency`-class real rows whose menu_item only composes on later frames, (3) garbled OCR rows (`LOu`); proven pre-existing (identical Unknowns in pre-repair run-1 child frames — masked by the normalizer Unresolved terminal), not introduced by the normalizer repair; Owner = Perception/Fusion + Semantic patterns (not normalizer); candidate gates: semantic suppressive default for text-less non-interactive fragments / fusion row-composition stability; Phase 2.6 STOPPED |
| `PHANTOM-FRAGMENT-ORIGIN-DIAGNOSTIC-RESULT.md` | `PROJECT_LEADER_PHANTOM_FRAGMENT_ORIGIN_DIAGNOSTIC_GATE` result: the 6..13 per-frame `type=NonInteractive, text="", StableKey=null` fragments are **NOT independent UI objects / not detector noise** — they are `row-relation-head` **satellites** (`relation_head_band_N_sat_K`, role=control) whose geometry/OCR are REAL YOLO `text_block` detections already consumed by the composed band (`headId` + `allIds`) — the same raw detection is double-published; canonical publishing drops the parent link → each satellite becomes an independent Unknown occurrence. Classification: **B. SUPPORTING_FRAGMENT** (real fragments of known rows; no A/C/D/E evidence); exact FDP = fusion publication boundary (`INTERNAL_COMPOSITION_ARTIFACT == CANONICAL_WORLD_OCCURRENCE`); reusable primitives = fused `evidence.headId` + C# `SemanticComposition.TryVerifyChild`/ChildOf relation; repair candidates (NOT authorized): fusion publication boundary (don't publish satellites as top-level occurrences) or headId→SupportingOf relation; Safety/OCR real-row gaps (`Safety & emergency`, `LOu`) DEFERRED separately; Phase 2.6 STOPPED |
| `FUSION-PUBLICATION-BOUNDARY-REPAIR-RESULT.md` | `PROJECT_LEADER_FUSION_PUBLICATION_BOUNDARY_REPAIR_GATE` result: row-relation-head satellites are `INTERNAL_SUPPORTING_FRAGMENT` (marker + headId + raw source consumed by owning band.allIds + no interaction evidence) and are now excluded from the top-level fused publication (`fuse_evidence`/`from_crops` boundary, new `fusion/publication.py`), while remaining observable in operator trace / fusionStages / `_diagnostics["internalSatellitesSuppressed"]`; frozen invariants (`INTERNAL_COMPOSITION_ARTIFACT != CANONICAL_WORLD_OCCURRENCE` etc.) coded. RED→GREEN 24+22 (exact falsifier = real child det_12/det_15 geometry); counterexamples 2–6 fail-closed preserved; perception suite 312/3 (same pre-existing 3), C# suite 2329/5 env-only, all zero new regressions; fresh real r3/r4: top-level satellites **0 on every accepted frame**, phantom-fragment Unknown = 0, child menu_item rows intact (13/frame, band allIds preserved), residual first blocker now = text_block-fragment verdict variance (`SEMANTIC_PATTERN_PREDICATE_FACT_FRAGMENTATION`, upstream / ICON_TEXTLESS) still fail-closed; receipt re-minted live (pipelineRevision updated); Phase 2.6 STOPPED |
| `SEMANTIC-FRAGMENT-VERDICT-CONSISTENCY-DIAGNOSTIC-RESULT.md` | `PROJECT_LEADER_SEMANTIC_FRAGMENT_VERDICT_CONSISTENCY_DIAGNOSTIC_GATE` result (diagnosis only): same-row text_block verdict instability (Unknown / NavigationCandidate / NonInteractive) traced with a /tmp semantic replay on the real r4 child frame — **`IsDuplicatePrimaryRowRendering` is structurally dead under the V2 projector's fact split** (Text/ClassName/Geometry are separate facts, but the peer query requires ONE fact to carry RawText + Bounds + menu_item provider ⇒ peers==0 always); the Nav-vs-Unknown split is solely structured-XML corroboration availability; '83%'-style NonInteractive comes from Pattern-7, not duplicate suppression; unit-test stubs (single-fact with text+bounds+provider) hid the gap (fixture blind spot). Classification **A. FACT_FRAGMENTATION (primary) + C. REPRESENTATION_ROLE_AMBIGUITY**; reusable primitives exist (fusion band allIds identity, canonical StableKey row_id, per-OccurrenceId grouping); Disposition = MINIMAL_REPAIR_GATE candidate (`PATTERN_5_OCCURRENCE_GRANULARITY_REPAIR_GATE`: occurrence-grouped peer aggregation + projector-shaped tests); no composition authority / contract change needed; Phase 2.6 STOPPED |
| `PATTERN-5-OCCURRENCE-GRANULARITY-REPAIR-RESULT.md` | `PROJECT_LEADER_PATTERN_5_OCCURRENCE_GRANULARITY_REPAIR_GATE` result: `IsDuplicatePrimaryRowRendering` repointed from fact-level to **occurrence-level aggregation** (`OccurrenceSemanticView`: occurrenceId/rawText/bounds/providers, capability-internal only; duplicate conditions unchanged — unique menu_item peer + same text + existing overlap predicate). RED→GREEN 34/34 (production-granularity buyer RED on the dead predicate → GREEN; counterexamples B–J fail-closed preserved; legacy mega-fact tests kept as compatibility); replay on the real r4 child frame: all 10 same-row text_blocks → NonInteractive (incl. the 4 previously XML-corroboration-promoted Nav); full C# suite 2344/5 env-only, zero new; fresh real r5 accepted child seq25: **eligible Unknown 8→2 (only no-peer 'Not set'/'Will never'), tb→Nav 0, tb→NonInteractive 11**, root complete (sources=17); next blocker moved to quiescence 'left container' (viewport-acceptance class, not this gate); debug buyer sample registered (Pattern-5 Fact Granularity Mismatch, TraceRef/EvidenceRef/AssetRef/replay refs); Phase 2.6 STOPPED |
| `QUIESCENCE-PAGE-CONTINUITY-DIAGNOSTIC-RESULT.md` | `PROJECT_LEADER_QUIESCENCE_PAGE_CONTINUITY_DIAGNOSTIC_GATE` result (diagnosis only): r5 `left container` at seq28 = **A. REAL_CONTAINER_EXIT, runtime classification CORRECT, fail-closed correct** — between seq27 (Display bottom, pending/multiplicity-mismatch — correct) and seq28 the REAL screen returned to the Settings ROOT (live structured UI dump: `search_action_bar` root marker + complete root rows; seq28 vision was an empty frame but did NOT drive the decision — the LeftContainer predicate `page != container` fired on the legitimate root marker via `SettingsStrategyBinding.ResolveSemanticPage` L163-166); ScrollForward swipe is 70%→30% height (outside the OS gesture band — gesture-trigger hypothesis weakened); one real scroll + zero intervening actions → app self-returned to root — trigger unidentified (MISSING_ASSET: screen recording/logcat window). FDP = world event, NOT a runtime evidence/decision defect; continuity predicate matrix (title/rows/signature CHANGED, foreground UNCHANGED); frozen invariants hold (no historical evidence in authority; exit proven by active evidence, not timeout); Disposition = EVIDENCE_COLLECTION (screen recording + logcat window; no runtime/authority repair indicated); toolchain buyer gaps recorded (time-window logs, causal subtree, AssetRef persistence); Phase 2.6 STOPPED |
| `REAL-CONTAINER-EXIT-CAUSE-EVIDENCE-COLLECTION-RESULT.md` | `PROJECT_LEADER_REAL_CONTAINER_EXIT_CAUSE_EVIDENCE_COLLECTION_GATE` result: evidence collection for the r5 `left container` exit — **5 controlled same-type campaigns reproduced 0 exits (0/5; all normal child exhaustion + completeness-Unknown); historical r5 remains the only instance (1/6 → ONE-OFF/INTERMITTENT)**. Classification **I. UNKNOWN** (repro exists but device-input/lifecycle/recording evidence missing; A excluded by action vocabulary, B by translator, C geometrically (swipe 540,1680→540,720, far from gesture zones); D/E/F/G unproven, no guess); runtime decision confirmed CORRECT (real exit, structured root marker). Delivered capture tooling: validation-side observation timestamp tap (`P26_TIMESTAMPS`: seq↔UTC↔run-relative) + capture driver (screenrecord segments/logcat/anchor) + per-run AssetRefs (stage/frames/timestamps/logcat ✓; SCREENSHOT/LOGCAT of the exit instance MISSING; screenrecord moov defect registered with fix). Logcats of 5 controlled runs: 0 FATAL/0 ANR/0 back-invocation/0 recreate. Disposition = EVIDENCE_COLLECTION continues (no speculative repair); toolchain buyer gaps recorded; Phase 2.6 STOPPED |
| `ROW-BAND-SUB-ELEMENT-BOUNDED-REPAIR-RESULT.md` | Option-A bounded-finish repair result: new `ROW_BAND_SUB_ELEMENT` semantic predicate (occurrence-level; sub-line contained in / caption directly below a UNIQUE menu_item row, different text, size + toggle + XML-peer + same-text guards — fail-closed) resolves the 'Not set'/'Will never' completeness-Unknown residuals. RED→GREEN 41/41 (2 buyers RED, P5 C/E counterexamples preserved by guards); full C# suite 2351/5 env-only; fresh real runF: **child Display discovery epoch first completes — `inventory complete: sources=17, unresolved=0, seq=[22,25,28,31]`, branch inventory 16 anchors**, terminal advances to the next layer: `Post-completeness fresh evidence INVALIDATED (previously-unknown nav:14)` — fail-closed correct, awaiting Leader acceptance-or-park decision; Phase 2.6 STOPPED |
| `POST-COMPLETENESS-NAV14-TIER-SCOPE-DIAGNOSTIC-RESULT.md` | Option-A bounded-finish final link: runF's `Post-completeness INVALIDATED (nav:14)` diagnosed — **nav:14 = AuxiliaryStructured (XML) candidate 'Dark theme'**; FDP = `PostCompletenessConsistencyValidator.Validate` B.3 iterates ALL `OccurrencesOf` (no eligibility filter) as fresh obligations against a vision-only frozen class set, and structured signatures (`RawText|Class|Resid|CD`) are never present there by construction ⇒ guaranteed false invalidation for any fresh frame carrying XML navigation rows (provable tier mismatch, not a genuine new source). Minimal repair candidate (NOT authorized, for decision): scope B.3 to `EligibleForAuthorization` occurrences (mirroring the epoch's own authority scope — eligible fresh candidates still all must resolve, fail-closed kept). Decision point A1 (authorize this small gate) vs A2 (declare phase result with this catalogued, provably-false invalidation); Phase 2.6 STOPPED |
| `POST-COMPLETENESS-TIER-SCOPE-REPAIR-RESULT.md` | Option-A1 final small gate: `PostCompletenessConsistencyValidator.Validate` B.3 scoped to `EligibleForAuthorization` occurrences (mirroring the vision-only frozen epoch; auxiliary XML rows are corroboration-only and their signatures can never be frozen — the runF 'nav:14' false invalidation). RED→GREEN 3/3 (PCC-16 runF replica FAIL on old → GREEN; PCC-17 new-eligible still invalidates; PCC-18 control) + `PostCompletenessConsistencyTests` 18/18; full C# 2354/5 env-only; fresh real runG: root 16 sources complete, terminal = transition-settle budget (device cadence, not a regression). Registration of the deterministic-blocker ledger (all session items cleared: normalizer order / phantom satellites / Pattern-5 granularity / row-band sub-elements / post-completeness tier-scope); Phase 2.6 STOPPED, awaiting Leader B1 (declare phase result) vs B2 (chase run-to-run all-green) |
| `PHASE-2.6-STAGE-DECLARATION.md` | Option-B1 stage declaration (per Leader): deterministic blockers of Phase 2.6 all cleared (ledger: normalizer order / phantom satellites / Pattern-5 granularity / row-band sub-elements / post-completeness tier-scope, each with evidence refs); real root & Display child discovery epochs proven (`sources=16/17, unresolved=0`; child `sources=17, seq=[22,25,28,31]`); residuals registered with owners & candidate gates (transition-settle cadence, intermittent exit I. UNKNOWN with capture pipeline ready, ICON_TEXTLESS / OCR / Safety-family / StableKey drift / Not set-Will never edge shapes all catalogued); explicitly NOT claiming PASS/graduation (terminal=Completed was not observed run-to-run; independent graduation review still pending); Phase 2.6 STOPPED, awaiting Human formal close/archive or residual-gate dispatch |
| `N-graduation-readiness.md` | Independent spec→test→evidence map + memory learning inputs |

WorkItem record: each worker's returned WorkResult is appended verbatim to the
corresponding letter file together with the leader's independent verification
(build + test re-run + `git status` purity check).
