# V6.10.1 调试工具与 BRANCH 处理测试增强 - 提案

> **变更**: v6-10-1-debugging-tools
> **创建日期**: 2026-06-08
> **状态**: 提案阶段
> **优先级**: P0

---

## 1. 问题陈述

在修复 `test_settings_simulation` 问题时，调试过程耗时 2-4 小时，主要困难包括：

| 类别 | 具体问题 | 影响 |
|------|----------|------|
| **架构复杂** | 4个组件交互（Engine、FSM、Matcher、Fixture） | 难以定位问题所在层 |
| **Trace 数据大** | 501步产生2000+条记录，关键信息分散 | 分析耗时长 |
| **工具不足** | 缺少堆栈显示、状态可视化 | 调试依赖脚本 |
| **假设验证慢** | 每次"假设→修改→测试"需5-10分钟 | 定位周期长 |

### 根本原因

核心问题在 `src/state_machine/traversal_fsm.py` 的 `_handle_branch` 方法对 DYNAMIC_MATCH 节点处理错误：

```python
# 错误代码（Line 1786-1790）
elif current_node.children_strategy.type == ChildrenStrategyType.DYNAMIC_MATCH:
    has_unvisited_children = True  # ❌ 总是假设有未访问子节点
```

**后果**：Wi-Fi menu_container 处理完 switch 子节点后，BRANCH 状态仍返回 NODE_SELECT 而不是 FRAME_COMPLETE，形成无限循环。

---

## 2. 解决方案概述

### 核心方案

创建调试工具和测试覆盖，将问题定位时间从 2-4 小时降低到 < 30 分钟：

1. **状态堆栈查看器**：实时显示状态机堆栈和最近状态转换
2. **决策点 Trace 增强**：关键决策点包含完整上下文信息
3. **BRANCH 处理单元测试**：覆盖 DYNAMIC_MATCH 边界条件
4. **完整遍历集成测试**：端到端验证深度优先遍历

### 预期成果

| 维度 | 当前状态 | 目标状态 |
|------|----------|----------|
| **调试效率** | 问题定位需 2-4 小时 | < 30 分钟 |
| **可观测性** | 关键决策无记录 | 所有关键点有 trace |
| **测试覆盖** | ~60% | > 85% |

---

## 3. 范围

### 包含内容

- 新建 `StateStackViewer` 调试工具类
- 在 `GraphTraversalEngine` 中添加 `_record_decision()` 方法
- 创建 BRANCH 处理单元测试（6个场景）
- 创建完整遍历集成测试
- 更新 `dashboards/README.md` 说明工具定位

### 排除内容

- 修改状态机核心逻辑（V6.10.2 处理）
- 代码质量重构（V6.10.3 处理）
- 文档编写（V6.10.4 处理）

---

## 4. 依赖关系

### 前置依赖

- **V6.9 plan-compilation-and-matching**：动态匹配和模板注册表已实现

### 后续变更

- **V6.10.2**：状态机逻辑优化（依赖本变更的调试工具）
- **V6.10.3**：代码质量改进（依赖本变更的测试覆盖）
- **V6.10.4**：调试文档（可并行开始）

---

## 5. 成功标准

### 功能验证

- ✅ `StateStackViewer.show_stack()` 能正确显示堆栈深度、节点名称、状态信息
- ✅ `StateStackViewer.show_transitions()` 能显示最近 N 条状态转换
- ✅ Trace 记录包含所有关键决策点的完整上下文
- ✅ BRANCH 处理单元测试覆盖 DYNAMIC_MATCH 边界条件（6 个场景）
- ✅ 完整遍历集成测试无无限循环，访问所有主要菜单项

### 代码质量

- ✅ 通过 mypy strict 类型检查
- ✅ 所有方法有完整类型注解
- ✅ 通过 ruff linting（零警告）
- ✅ 符合 CLAUDE_CONVENTIONS.md 要求

### 测试覆盖

- ✅ BRANCH 处理测试覆盖率 > 90%
- ✅ 完整遍历测试覆盖率 > 85%

---

## 6. 实施估算

- **预计工时**: 5 小时
- **实施阶段**: P0（立即见效）
- **风险等级**: 低（新增工具和测试，不影响核心逻辑）

---

## 7. 提案历史

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-06-08 | 1.0 | 初始提案 |
