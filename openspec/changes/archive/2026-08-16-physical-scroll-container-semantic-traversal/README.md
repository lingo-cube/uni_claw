# physical-scroll-container-semantic-traversal

Agent-controlled same-Container viewport scroll to a below-fold switch (Developer options → Automatic system updates), reusing the graduated semantic loop plus the frozen SC-P3-003 / SC-P3-CAND-007 viewport machinery
