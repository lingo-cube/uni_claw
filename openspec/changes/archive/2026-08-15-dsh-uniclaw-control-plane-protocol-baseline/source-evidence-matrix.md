# SourceEvidenceMatrix — DSH Control-Plane Surfaces

> Change: `dsh-uniclaw-control-plane-protocol-baseline`
> Pinned DSH baseline: `UNICLAW_DSH_COMPATIBILITY_BASELINE = 47f943859bef60e4160492346772ded9b24f765a`
> DSH version: `0.1.0-rc.5` (pre-release, no git tags on the pinned checkout; release commit `abe560f81e` in history)
> Remote: `https://github.com/deepseek-ai/deepseek-harness.git`, branch `master`
> Audit method: READ-ONLY. Every row verified against the pinned checkout at `/Users/fran/Documents/Code/dk-harness` (commit `47f943859b`). SOURCE IS AUTHORITATIVE; docs discrepancies are recorded per row. Line numbers reference the pinned checkout and may drift on later commits — the File + Type/API/Event columns are the stable identity.
> Row count: 43 rows (S1–S43) + 11 docs-vs-source discrepancies (D1–D11).

## Matrix

| # | DSH Concern | Source File (pinned checkout) | Type / API / Event | Semantics | Durable? / Model-facing? | Lifecycle | Stability | UniClaw Use |
|---|-------------|-------------------------------|--------------------|-----------|--------------------------|-----------|-----------|-------------|
| S1 | Session event vocabulary | `packages/core/session/src/types.ts`; `packages/core/session/src/known-event-types.ts` | `SessionEventMap` (line ~236), merge-extensible via `declare module '@deepseek-ai/dsh-session/types' { interface SessionEventMap { ... } }` (pattern: `packages/core/agent/src/types.ts`); `KNOWN_SESSION_EVENT_TYPES` (**GENERATED**, **44** members, read path refuses unknown non-`ignorable` types); `SESSION_FORMAT_VERSION = 0` pinned (types.ts:56, no migration — version refusal) | Discriminated union of durable per-session event types; any plugin package may extend it with its own event kinds | Durable (append-only log); not model-visible unless consumed | Registry-merge; per-session log | Merge pattern documented; pre-release (no compatibility promise); catalog verified by `verify-persistence-catalog` | Native seam for a dsh-plugin-uniclaw-declared Kernel-fact event vocabulary (e.g. `uniclaw/run-event`, `uniclaw/run-snapshot`); plugin owns the merge declaration |
| S2 | Session event envelope | `packages/core/session/src/types.ts` | `SessionEvent` (line ~404): `{ type, seq, time, data, ignorable?, sourceEventSeqs?, surfaceOp? }` | Immutable, JSON-serializable, seq-stamped envelope; `ignorable: true` marks loss-tolerant informational records | Durable | Append-only | Frozen contract; `SESSION_FORMAT_VERSION = 0` | Envelope a plugin reuses when appending Kernel-derived facts; `ignorable` lets high-volume telemetry degrade safely |
| S3 | Session append + immutable log | `packages/core/session/src/index.ts` | `Session` (plain class, line ~425, event-sourced: append-only `SessionEvent[]` log, `seq = log.length` contiguity, lossless-JSON snapshot per append); `Session.append<T>(type, data)` (line ~604); `Session.events` immutable snapshot; `Session.deriveMessages()` (line ~726) — LLM history is **derived, never stored** (surface-op-marked nodes folded via `deriveEventMessage`, cached per node, generation-invalidated); `session-invariant` plugin enforces relational integrity (seq/turn/step nesting, tool/result pairing) | Append-only validated log; `append` deep-freezes, validates via `SurfaceManager.validateNext`, dispatches post-commit `session/event` firehose with reentry guard; validates JSON-serializability; stamps seq | Durable | Per-session; disposed with session | Stable; "model-visible means logged" | Only sanctioned writer path for plugin-owned session events; MUST NOT be used to fabricate Kernel facts (authority plane) |
| S4 | Session service + fork | `packages/core/session/src/index.ts` | `ctx.sessions.create(id?, options?)` (line ~830), `.list()`, `.fork(source, boundary?, childSessionId?)` (line ~1087); `_forkSeed` rejects boundaries inside an open turn (`OPEN_TURN`); `_resolveForkSource` (`SESSION_NOT_FOUND`/`SESSION_NOT_LIVE`); `SessionForkError` codes | In-memory session registry; fork copies stable prefix through inclusive boundary; child header carries durable `parentSession` + `seedLength` (survives restart); durable seed boundary also published as `session/end-seed` event (`Session.firstLiveSeq`) | Durable (fork lineage) | Per-session | Stable | Run→session binding; a Kernel run may map 1:1 to a DSH session; fork lineage usable for run-branch semantics |
| S5 | Live session event firehose | `packages/core/session/src/index.ts` | `session/event` emit-mode event (line ~76); `session/created` (sync veto+rollback), `session/disposed`; `ctx.sessions.flush(session)` — `session/flush` awaited-parallel barrier (declaration line 85, impl `async flush(session): Promise<boolean>` line 1022); per-listener containment (`invokeContainedSessionObservers`, fire-and-forget) | Every committed session event broadcast live to listeners; **session events are NOT cordis events — one in-process source fans out over three delivery paths**: in-process `session/event` listeners (persistence, telemetry), SDK server JSON-RPC re-emit (`session.event`), apiproxy mux WebSocket push | Live (not itself durable — fires as events commit) | Per-session; listeners auto-disposed with fiber | Stable | Plugin hooks `ctx.on('session/event', ...)` to see plugin-appended facts live; wire push to UI via apiproxy mux |
| S6 | Agent live event surface | `packages/core/agent/src/runtime-types.ts` (events ~159–290), `packages/core/agent/src/index.ts`, `packages/core/agent/src/types.ts:19` | `agent/created`, `agent/disposed`, `agent/status`, `agent/inbox/inserted`, `agent/inbox/claimed`, `agent/inbox/discarded`, `agent/session-start`, `agent/pre-step` (waterfall), `agent/request` (waterfall), `agent/request-error` (waterfall), `agent/turn-stopping` (serial), `agent/error`; dispatched via `Scoped<Agent>`; durable counterpart `agent/inbox/spliced` (every inbox mutation logged); `withInitiator`/`requireInitiator`/`withoutInitiator` causal attribution | Live per-agent lifecycle/status/loop events; two waterfalls delegate turn/request control; `AgentHandle.dispose()` is a genuine capability; `AgentStatus` is `'idle'|'running'` only (disposal is NOT a status) | Live; not durable (inbox mutations have durable `agent/inbox/spliced` counterpart) | Per-agent scope; listeners disposed with owning fiber | Stable; documented | Shadow insertion point candidates (`agent/status`, `agent/pre-step`); plugin lifecycle hooks (`agent/created`, `agent/disposed`); initiator attribution for driver chains |
| S7 | Commands registry (deterministic human control) | `packages/interaction/commands/src/index.ts` | `CommandRuntime extends TypertRemoteService` (line ~225); `CommandDefinition { name, description, input?, handler }` (line ~40); `CommandInvocation` (line ~28); `ctx.commands.register(def): () => void` (line ~245) | Plugin-owned slash-command registry; handler executes **against the receiving agent WITHOUT sending the command to the model** — deterministic human control, zero LLM tokens; mints `CommandId`, durable `command/run`+`command/done`, live `commands/change` emit; thrown/aborted → `{kind:'error'}` | `command/run` + `command/done` durable session events; `command/run` may record `rawInput` | Per-scope layer; per-agent variant mounts under `agent.ctx`; disposer unregisters | Stable | PRIMARY native seam for human control ops: Start Run / Pause / Resume / Stop / Abort / Inspect Run / Inspect Trap / Inspect Evidence — deterministic, no model turn |
| S8 | Command source + vocabulary extension | `packages/interaction/commands/src/types.ts` | `CommandSourceMap` (line ~57, merge-extensible); SessionEventMap merge (line ~77) for `command/run`/`command/done` | Durable command lifecycle events; source map merge-extensible | Durable | Per-session log | Stable | Plugin may extend source map if a non-`user` command origin appears; lifecycle events give audit trail |
| S9 | Tools registry (model-facing) | `packages/core/tools/src/index.ts`, `packages/core/tools/src/schema.ts` | `ctx.tools.register(definition: ToolDefinition): () => void` (line ~1037); `ToolDefinition` (line ~222) extends `ToolSchema { name, description, parameters?, output { schema, render, presentationMeta? }, execute(args, exec: ToolRunContext) }`; `defineTool` (schema.ts ~545, params→JSON Schema, validates args, `ToolArgsError`); reserved name `run_code` (line ~1055); `schemas()` (~1234) projects **only `name/description/parameters`** to the model (`schemaOf` ~1256; execution/presentation callbacks never sent); `executionMode` (~1276) fail-closed parallel/exclusive; scheduler (`agent-loop/src/tool-calls.ts`): exclusive barrier vs parallel pool (`DEFAULT_MAX_PARALLEL_TOOL_CALLS = 10`), aborted batches record synthetic error results, terminal scheduler failure preserves `tool/call` without fabricating results | Model-facing tools with JSON-schema parameters, structured output schema + render intent, cancellation via `exec.signal`, `timeoutMs`, `finalizeContent?` | Tool calls/results durable (`tool/call`, `tool/result`); tool itself is registry state | Scoped registration shadows globals; disposer per registration; per-agent scope via preset | Stable | Native seam for any future model-facing `uniclaw.*` tool; each tool requires Buyer/Purpose/Input/Output/Freshness/Kernel-validation before approval (F-tool table in design.md) |
| S10 | Tool guard waterfalls + timeout | `packages/core/tools/src/index.ts`, `packages/guard/timeout-policy/src/index.ts` | `tools/pre-execute` (waterfall → `PreToolDecision`), `tools/execute` (waterfall → `ToolExecutionResult`, timeout/retry/metrics), `tools/post-execute` (waterfall → `PostToolDecision`); timeout guard: `TOOL_TIMEOUT` (line ~25), `tools/execute` listener swaps `exec.signal` to a `deadline()` signal and replaces the result with `toolTimeoutResult(timeoutMs)` only when the timeout fired (driven by `ToolDefinition.timeoutMs`); abort codes `TOOL_ABORTED`/`TOOL_ABORTED_BEFORE_DISPATCH` (lines ~469/472) | Extension points before/around/after tool dispatch; guards can block or re-route; timeout enforcement lives in a separate guard plugin, not the registry field | Live | Fiber-scoped listeners | Stable | Permission gate for Kernel-facing tools; pre-execute guard = native place to enforce authority boundary |
| S11 | Client module registry (browser half) | `packages/client/modules/src/index.ts`; `packages/client/web/src/boot.tsx`; `packages/client/modules/src/client/system.ts`; `packages/client/connection/src/*` | `ctx.clientModules` (`ClientModuleRegistry extends Service`, inject `['webServer','loader']`, ~184–204); `dsh.client = { platform:'web', inject?, immediately? }` package.json field + `exports["./client"]` bundle (validated by `parseDshClient`/`clientExportOf` ~109–142); host composes wire graph (`compose()` ~315), serves `/plugins/<id>/client.js?rev=<rev>` (rev = 12-hex sha1) and taps `index.html` to inject `window.__DSH_BOOT__` (`injectBootManifest` ~168); browser half = `WebBootEntry {id,url,rev,inject?,immediately?}` graph → lazy-CJS `ClientModuleSystem` (`ctx.modules`; kernel writes `window.__DSH_MODULES__` before Cordis exists — bootstrap exception) → one vendored-Cordis Loader entry per plugin row + `APP_SHELL_ID` pseudo entry, `loader.await()` + fail-fast fiber sweep | Declarative host→browser module graph composing `window.__DSH_BOOT__` | Client registry state; per-browser-session | Roster-driven; rev-versioned | Stable; loud composition errors (`ClientPackageCompositionError`, `MissingClientBundleError`) | dsh-plugin-uniclaw declares its browser half here to mount Control Plane UI |
| S12 | Client UI slots | `packages/client/ui-slots/src/index.ts`; `packages/client/runtime/src/client/slots.ts`; `packages/client/runtime/src/client/contract/conversation.ts` | `SlotMap {}` (merge-extensible, line ~24); `SlotKind = 'single'|'list'|'keyed'|'chain'` (88); `SlotScope = 'root'|'session-maybe'|'session'` (91); `SlotEntryDef/SlotSpec/ChildrenDecl` (100/130/145); runtime `SlotRegistry extends Service` (`ctx.slots`, slots.ts:93–105) declares `'root'`, `install(renderer)` (213), `snapshot(root?) → LiveSlotNode[]` (308), emits `'slots/changed'` (106); `ConversationNodeDefinition` (conversation.ts:171); consumers: ui-layout (`sidebar`/`conversation`/`details`/`shell.overlay`, AppFrame into `'root'`), ui-settings (`settings.close`/`section`/`plugins.tab`/`onboarding`/`general.item`), ui-theme (`appearance` row); theme seam: `ctx.theme`, `ThemeDefinition {id, base?, tokens}`, `--dsw-alias-*` tokens, `'theme/change'`; example `packages/client/ui-trajectory/src/client/index.ts:43` (`conversation.view`, `inject: ['slots','conversationEvents','conversationViews','sessions','locale']`) | Declarative UI extension points; registration rides effect wrapper so unload removes the panel | Client state; session-scoped | Fiber-scoped | Stable; example-backed; slot catalog is generated (`gen-client-catalog.ts` → `CLIENT_SLOT_API`, freshness-gated by `verify-client-catalog`) | Control Plane UI panel home (conversation.view-style slot); RunSnapshot/Evidence UI as a session-scoped slot |
| S13 | Typert Remote gateway | `packages/api/gateway/src/index.ts` (`dispatchRpc` line ~108, `invoke` line ~145); `packages/typert/protocol/src/index.ts` (`TypertRemoteService` line ~147, `@Remote()` decorator, `bindTypertRemote`) | Live Typert Remote dispatch over Cordis Services; any Service extending `TypertRemoteService` exposes typed methods to browser/client and other RPC consumers | Read-model service surface; host services callable from browser | Service state (in-process) | Service lifecycle | Stable; used by GoalService, CommandRuntime, plugin-inventory | RunSnapshot read-model service (`UniClawRunService extends TypertRemoteService` with read-only `remoteExport*` methods) — the canonical read path for the browser UI |
| S14 | Forwarded host events allowlist | `packages/api/remotes/src/remote-events.ts` | `API_REMOTE_FORWARDED_EVENTS`: `agent-preset/selected`, `commands/change`, `credentials/updated`, `cordis/request-run`, `cordis/request-run-resolved`, `cordis/dynamic-package`, `cordis/dynamic-retract`, `cordis/inspect-query`, `cordis/inspect-query-resolved`, `llm/adapters-updated`, `settings/document-updated` — wire name = Host event name, payload = arg list | Explicit allowlist of Host events pushed to remotes; **`session/event` deliberately absent** | Live | Allowlist is composition state | Stable | Plugin-declared host events reach the browser ONLY via this allowlist; a plugin wanting live Kernel-fact push must add its event here (or use session/event + mux which already flows) |
| S15 | Web transport | `packages/client/connection/src/{api-path.ts, http-bridge.ts, websocket-downlink.ts, api-request-trust.ts, rpc.ts, rpc-host.ts}` | `API_PATH = '/api'`; `http-bridge` (node:http ↔ fetch); `WebSocketDownlinks` pumps `/api/events.mux` + `/api/events.host` (`MUX_EVENTS_PATH`/`HOST_EVENTS_PATH`, api-path.ts); browser `readWebSocket(MUX_EVENTS_PATH, signal, muxFrameSchema, onOpen)`; unary/respond calls use fetch | In-process HTTP/WS transport between browser and Host; RPC + event stream | Transport | Per-connection | Stable | Existing plumbing; a plugin adds UI + Remote methods without new transport work |
| S16 | Host API frames | `packages/host/apiproxy/src/api/events.ts` | `EventsApi.mux` (all-session aggregated stream) + `EventsApi.host` (session create/destroy, running-status flips, agent failures, workspace/archive); `MuxFrame` (incl. `session/event` raw passthrough + optional host-computed `ToolEventView` — pure derivation, never persisted, `session/subscribed` (sessionId + `lastSeq = seq−1` late-subscriber replay), `approval/requested`/`resolved`, `question/requested`/`resolved` (answerable rpcId reused verbatim), `session/queue`, `session/jobs`, `session/projection`, `host/remote-event`, `stream/error`); forwarded host events pass `assertJsonArgs` (lossless-JSON gate, throws on non-lossless args); `muxQueues: Set<FrameQueue<RpcRequest<MuxFrame>>>`; `ctx.on('session/event', ...)` pushes frames with view projection; `subscribeSession` emits the subscribed control frame | Wire frames pushed to the browser: raw session-event passthrough + control frames + **`session/projection` live push of projection-unit values** | Live push; never logged (replay recomputes on host) | Per-mux-consumer | Stable | `session/projection` = native live read-model push channel for a `uniclaw.runSnapshot` projection unit (UI freshness without polling); `session/subscribed` gives late-subscriber replay (seq−1) |
| S17 | Session projection registry | `packages/session/session-projection/src/index.ts` | `SessionProjectionRegistry` (`ctx.sessionProjections`); `SessionProjectionMap {}` merge-extensible (types.ts); `register(definition: ProjectionDefinition)` (line ~194); `ProjectionDefinition { key, schema, init, apply, view, stateVersion }`; `onChanged(listener)` | Pure fold over committed session events → validated wire payload; persisted-cache checkpoint `(sessionId, key, ver, seq, val)`; live `session/projection` frames; **a projection is a read model, never authoritative** | State is fold-derived; cache persisted in storage domain `session_projcache` (v3) | Effect registration (disposer); per-session cells | Stable; versioned by `stateVersion` | RunSnapshot/run-status as a projection unit folded from plugin-appended session facts — read-only, derived-where-marked, truth-source preserving; avoids a second mutable DSH-owned Runtime state |
| S18 | Storage domain seam | `packages/storage/storage-domain/src/spec.ts` | `defineDomain({ name, version, global?, tables })`; `domainTable` zod schemas; `DomainFacility` | Declarative durable storage domains; backends `storage-json` / `storage-sqlite` | Durable | Domain registry | Stable | If plugin needs persisted metadata (run registry, EvidenceRef index), it declares its own storage domain — no parallel persistence protocol |
| S19 | Session persistence seam | `packages/session/session-persistence/src/index.ts` + `coordinator.ts` + `write-behind.ts` + `repair.ts`; backends `session-persistence-jsonl`, `session-persistence-sqlite` | abstract `SessionPersistence extends Service` (`ctx.sessionPersistence`): `locate`, `supportsRawArtifacts`, `readRaw`, `create`, `append`, `prepare`, `load`, `inspect`, `list`, `listSnapshots`; `PersistenceCoordinator` + `PersistenceBackend` (`loadStored`, `readStoredRevision`, `appendBatch`, `commitRepair`, `list`): write-behind batching (`SessionWriteBehind`, default `DEFAULT_WRITE_BATCH_MAX_DELAY_MS=200`, barrier-flush quiescence, failure retention + `automaticPaused`), torn-tail repair commits synthetic `interruptedTurnClosers` (`TOOL_NOT_STARTED`, `TOOL_OUTCOME_UNKNOWN`); `SessionFormatUnsupportedError` (version refusal, **no migration**) vs `SessionPersistenceCorruptionError`; prepared-session LRU `DEFAULT_PREPARED_SESSION_CACHE_SIZE=5`; legacy v0 import mapping (steering/message→user/message, turn/start trigger, turn/end reason); `load(id): SessionInspection` (line ~183) — **load commits repair, `inspect` does not** (immutable, no recovery); backends: JSONL (`JsonlSessionPersistence` index.ts:121, `JsonlCompression='zstd'|'none'`, HeaderLine first record, retired `sandboxMode`/`approvalPolicy` header fields rejected, truncation-repair offset) + SQLite (`SCHEMA_VERSION=15`, app id `0x44534850`, STRICT `sessions`+`events` tables, FK CASCADE, WAL, `scanRows` torn detection) | Abstract durable session log persistence; append resolves after in-process commit; durable write-behind with `session/flush` awaited barrier; torn-tail repair never truncates mid-turn | Durable | Backend service | Stable | Kernel-fact session events persist with the session log; replay/resume reconstructs appended facts (buyer: audit/replay/resume) |
| S20 | Checkpoint policy | `packages/session/session-checkpoint-policy/src/index.ts` | inject `['llm','sessionPersistence','sessions','tools']`; `afterCheckpoint` awaits `ctx.sessions.flush(session)` **BEFORE yielding the first chunk**; listeners `llm/stream`, `tools/execute` (top-level only), `agent/pre-step`; aborted → `TOOL_ABORTED_BEFORE_DISPATCH`; fail-closed | Session log flush to persistence at deterministic boundaries (turn start) | Durable | Policy plugin | Stable | Plugin-appended facts ride the same flush discipline — durability for free |
| S21 | SDK (JSON-RPC) | `packages/sdk/protocol/src/transport.ts`, `types.ts`; `packages/sdk/server/src/server.ts`; `packages/sdk/client/src/client.ts` | `JsonRpcLineTransport` — newline-delimited JSON-RPC 2.0 over byte streams (malformed lines ignored; -32601/-32603 error mapping); `HarnessSdkRequestMap = { initialize, session/prompt, shutdown }` (types.ts ~101); `HarnessSdkNotificationMap = { session.event (full SessionEvent), session.status (idle\|running), subagent.started, subagent.finished }` (~93); wire-stable `serverInfo.name = 'deepseek-harness-sdk-runtime'`; `HarnessSdkJsonRpcServer` subscribes in-process `session/event`→`session.event`, `agent/status`→`session.status`, `session/created` (with parentSession)→`subagent.started`, `subagent/end` (scoped, `info.local`)→`subagent.finished`; `session/prompt` validates agent liveness in `ctx.agents`, lazily creates agent+session pair; plugin `sdk-jsonrpc-server` wires stdio, flushes transport on shutdown, disposes root fiber, exit 0; TS client `HarnessClient.subscribe(filter?)/subscribeSessionTree(sessionId)` → `AsyncIterable<HarnessNotification>`, `disposeRuntimeProcess` | External-process automation seam: DSH as a server an external process drives; no streaming REQUEST — streaming is notification-based (`session.event` per appended log event incl. raw `assistant/chunk`) | Stream transport | Per-connection | Stable | NOT the control-plane direction (DSH must not become an SDK client to the Kernel); classify under transport-decision evidence (design.md §18) |
| S22 | ACP server | `packages/acp/acp/src/index.ts` | official `@agentclientprotocol/sdk` (`AgentSideConnection`, `ndJsonStream`, `PROTOCOL_VERSION`); `agentInfo { name: 'deepseek-harness-acp', version: '0.0.1' }` (line ~239); `makeAgent` creates FRESH sessions via `agents.create` (no preset composition), prompt→text; permission requests ride a machine policy channel; codec `turnEndToStopReason`/`acpPromptToText`/`promptHasUnsupportedContent` | Automation-only protocol server for machine clients; presentation stays with the web client | Transport | Per-connection | Stable | Same classification as S21: automation inbound seam, not Kernel control plane |
| S23 | Goal machinery | `packages/goal/goal/src/index.ts` | `GoalService extends TypertRemoteService` (line ~183): `get/disarm/create/edit/pause/resume/complete/block/clear` (+ `remoteExportCreate`); durable `goal/change` session events; `goal` projection unit (stateVersion 4) | Native goal lifecycle: durable goal/change events, projection unit for UI, Remote service for browser, tools (`tool:goal`, `get_goal`, `create_goal`, `update_goal` in tool-goal) and `/goal` command (command-goal) | Durable goal events; live view | Service lifecycle; per-agent cache | Stable | DSH-native goal machinery for UniClaw run goals IF the goal lifecycle should live in DSH — otherwise Kernel-owned goal stays Kernel-owned and DSH uses proposal/admission seams (§12) |
| S24 | Goal round driver | `packages/goal/goal-round-driver/src/index.ts` | `inject ['agents','goals','sessions']`; listens `agent/error`, `agent/created`, `agent/disposed`, `agent/session-start`, `agent/status`, `goal/changed`, `agent/inbox/*`, `session/event` | Same-session goal-round driver over public agent/session/goal services | Live | Plugin | Stable | Orchestration precedent for Shadow cognition rounds if Shadow uses DSH goal machinery |
| S25 | Workflow orchestration | `packages/workflow/workflow/src/index.ts`; `packages/workflow/workflow-worker-thread/src/index.ts`; `packages/workflow/tool-workflow/src/index.ts` | `ctx.workflowEngine` = abstract `WorkflowEngine extends Service` (single engine per context, **no registry** — asymmetric with subagents); `start(request: WorkflowStartRequest): WorkflowRun`; events (all emit) `workflow/start` (43), `workflow/phase` (51), `workflow/log` (58), `workflow/agent-start` (68), `workflow/agent-end` (79), `workflow/end` (89); `WorkflowStartRequest {script, meta, args?, subagentProvider?, maxTotalAgents?, parent: Agent, signal?}`; worker-thread engine (inject `['subagents']`, Config `{provider 'spawn', maxConcurrentAgents 0, maxTotalAgents 1000, maxItemsPerCall 4096, syncTimeoutMs 5000, disposeGraceMs 5000}`), body compiled `vm.Script` (no import/require), `export const meta` rejected, vm globals `agent/parallel/pipeline/phase/log/args`, `drive()` never rejects, `cancel()` → WorkflowError CANCELLED; tool `workflow` (params `{script, meta, args}`, output `{runId, agentsStarted, result}`); sibling tool `ralph` (fresh-agent loop) | Multi-agent orchestration: `agent()`, `pipeline()`, `parallel()`, `phase()`, `log()`, `args`; concurrency + total-agent caps | Script is per-call; orchestration live | Engine lifecycle | Stable | Future Shadow/multi-agent cognition orchestration seam; NOT needed for this baseline |
| S26 | Subagent registry | `packages/subagent/subagent/src/index.ts`; `packages/subagent/tool-subagent/src/index.ts`; `packages/subagent/tool-subagent-control/src/index.ts`; `packages/subagent/tool-subagent-report/src/index.ts` | `ctx.subagents` = `SubagentRuntime extends Service` (~126+): **named-provider registry** — `registerProvider` (369), `getProvider` (392), `list` (400), `start` (414), `startContinuable` (212), `followup` (231), `interrupt` (255), `reportFrom` (270), `registerContinuableSetup` (286), `drainContinuableDescendants` (304), `listChildren` (339), `listDescendants` (358); events `subagent/provider-added` (140), `subagent/provider-removed` (146), `subagent/start` (Scoped, 157), `subagent/end` (166); providers `SpawnInProcessProvider` (inheritsParentContext=false) + `ForkInProcessProvider` (inheritsParentContext=true); tools `subagent`, `send_message`/`interrupt_agent`/`list_agents`, `report` (child-scoped, Config `reportDelivery: 'quiet'|'wakeup'`) | Continuable subagents with durable child sessions; child-first disposal | Durable child sessions; live events | Per-agent | Stable | Delegated cognition seam (diagnosis/healing candidates as subagent work) — later phase |
| S27 | Composition model | `packages/bundle/base/cordis.patch.yml`; `packages/bundle/web-app/cordis.patch.yml`; `apps/cli`; `vendor/include/src/index.ts` (`applyEntryPatches`, `entryListSchema` = YAML + `!!js` `JsExpr` evaluated at activation vs `disabled` at mount); `vendor/loader/src/config/isolate.ts` | Profiles (named composition in Harness home listing bundles + `cordis.patch.yml` layers); bundles (`dsh` field in package.json; `dsh-base` first layer, `dsh-web-app`, `dsh-headless`); patch replaces whole row config by id or inserts rows; `isolate` realms re-home services on patch (`LocalRealm '#'+id` / `GlobalRealm '@'+label`); `dsh --profile web --dump-config`; `dsh.client` rows = browser roster → `window.__DSH_BOOT__` | Declarative composition of the whole Host + Client; patch layering | Composition state | Boot-time | Stable | dsh-plugin-uniclaw lands as composition rows (host plugin row + client module row + permission/sandbox rows where needed) — no new composition mechanism |
| S28 | Agent presets (per-session composition) | `packages/preset/agent-presets/src/{mount.ts, preset.ts, session.ts, authoring.ts, discovery.ts, types.ts}` | `${DSH_HOME:-$HOME/.dsh}/.agent-presets/<id>/` cordis.yml files; `ctx.agentPresets`; `AgentPresets.mount(agentCtx, id)`; `resolveSessionPreset(session)`; standing mounts; root-realm leak guards | Per-session agent composition: tools/persona/prompt sections mount into one agent's scoped world; a preset IS a composition; host plane vs agent preset plane split | Composition state | Per-session mount; unwinds with agent | Stable; shipped presets read-only system trust; `$DSH_HOME/.agent-presets` writable | Per-run agent composition (dsh-plugin-uniclaw rows per agent scope) OR host-plane plugin; decide in design.md §6 — two-plane rule: registries/shared → host; one-session tools/persona → preset |
| S29 | Agent creation/resume factory | `packages/core/agent/src/index.ts`; `packages/core/agent-loop/src/index.ts` | `ctx.agents.create(ownerCtx, CreateAgentOptions)` (options line ~80: `sessionId`, `meta {cwd, parentSession, origin, agentPreset, ...}`, `seed`, `agentOptions`, `setup` line ~132); `ctx.agents.resume(ResumeAgentOptions)`; `AgentHandle { agent, dispose() }` (line ~172); `AgentRegistry` (line ~256); resume identity: `resumeSessionId`/`resume` mutually exclusive with `sessionId`; `resumeWith(ctx, persistence, ...)`; `restoreOrCreateConfigured` — resume first, on failure checks `persistence.list()` existence → rethrow corruption if the session exists, else create fresh; `waitForDrainingConfiguredIdentity` (agent/disposed + session/disposed listeners); `agent-loop/config-start-failed` event; coordinator `load(id)` waits for the live in-memory snapshot to be durable (balanced), repair applies to cold sessions only | Factory creating a session+agent pair under one identity; `setup(agentCtx)` composes the scoped world BEFORE `session/created`/`agent/created` publication; `resume` rebuilds from persisted session (`Session.fromRestore` validates header+seed to the SAME invariants as append) | Durable session; live agent | Factory-owned | Stable | dsh-plugin-uniclaw uses `setup` to mount per-run scoped tools/commands/prompt sections; run=session identity policy; resume/replay reconstructs plugin-appended Kernel facts from the durable log |
| S30 | Boot chain | `packages/boot/app-boot/src/index.ts` | `boot() → Loader → mountRootInclude → assertEntriesActivated` | Ordered boot; activation audit; failed rows rejected | Boot state | Process | Stable | Plugin row activation is audited at boot; a misconfigured uniclaw row fails loud, never silent |
| S31 | Cordis plugin contract | `vendor/cordis/src/registry.ts`, `vendor/cordis/src/fiber.ts` | `Plugin.Function | Plugin.Constructor | Plugin.Object`; `Plugin.Base { name, Config (StandardSchema), inject, provide, intercept }` (lines ~92–146); `apply(ctx, config)`; `ctx.plugin(...)` creates a `Fiber` (states `PENDING/LOADING/ACTIVE/FAILED/DISPOSED/UNLOADING`); config validated once per activation via `resolveConfig` (StandardSchemaV1; async validation throws `TypeError`) | Core plugin shape; config validated before start; `inject` waits on services (unmet inject keeps fiber `PENDING`); `provide` declares services; every side effect is a fiber effect (disposers run in reverse on unload) | Plugin registry state | Fiber lifecycle | Stable; sole `@experimental` marker is `Context.root`; vendored Cordis is a locally modified fork (`vendor/README.md`: upstream cordis 4.0.0-rc.7 → package 4.0.1, loader 1.0.0-rc.5 → 1.0.2, schemastery 3.18.0 → 3.18.1, include 1.0.4 → 1.0.6) | dsh-plugin-uniclaw implements this contract; `Config` = DSH-side config (DriverHost endpoint etc.) |
| S32 | Cordis service + context | `vendor/cordis/src/service.ts`, `vendor/cordis/src/context.ts`, `vendor/cordis/src/reflect.ts` | `Service` constructor auto-`provide`; `ctx.provide/get/set/accessor/mixin`; Proxy-backed `Context`; `declare module '@deepseek-ai/cordis' { interface Context { ... } }` typed augmentation; `ctx.inject(deps, callback)` (array or `{key: intercept-config}` map, normalized by `Inject.resolve`; `@Inject` decorator); `ctx.effect` disposers | Service registration, resolution, typed augmentation; dependency waiting is service-availability-driven | Registry state | Fiber-scoped | Stable | dsh-plugin-uniclaw provides `uniclaw` service; consumers `inject: ['uniclaw']`; type augmentation documented |
| S33 | Cordis events core | `vendor/cordis/src/events.ts` | `EventsService`; `DispatchMode = 'emit' | 'parallel' | 'serial' | 'bail' | 'waterfall'`; `ctx.on/once/emit/broadcast`; listener auto-disposal with fiber | Event dispatch strategies; docs primer lists 4 modes, source has 5 (`bail` under-documented) | Live | Fiber-scoped | Stable (source); docs discrepancy recorded | Plugin-owned live events use `emit`; control delegation uses `waterfall`/`serial`; note `bail` exists for veto patterns |
| S34 | Sandbox + approval + permission | `packages/bundle/base/cordis.patch.yml` rows: `sandbox '@deepseek-ai/dsh-sandbox-local'`, `sandbox-policy`, `bash-sandbox`, `approval '@deepseek-ai/dsh-user-approval'`, `permission '@deepseek-ai/dsh-permission-presets'` config `{ sandbox: read-only, approval: ask }`; `packages/sandbox/*`, `packages/interaction/permission-presets/*`, `packages/interaction/user-approval/*` | Two orthogonal knobs (sandbox mode + approval policy) as pure session-event folds; `tools/pre-execute → serviceAsk → guardReason → tools/post-execute` gate; `serviceAsk` (tools/src ~1689) **fail-closes to deny** when no ApprovalService is composed or execution is agent-less; `ApprovalService.request` → `ApprovalOutcome = 'allowed-once'|'rejected'|'cancelled'|'unavailable'`; `approveEscalation` fail-closed; permission presets + `/permission` command + `permissions` projection | Deterministic machine policy channels; fail-closed | Policy state; session events | Plugin rows | Stable | Kernel-facing tools never bypass this gate; DSH→Kernel ops ride approval/sandbox policy (human consent at the DSH control plane), while Kernel remains the execution authority |
| S35 | Settings + config layering | `packages/settings/settings/src/index.ts`; `apps/cli` `--dump-config`; `packages/boot/app-boot/src/profile.ts`, `apps/cli/src/profile-boot.ts` | `ctx.settings` (`SettingsProvider 'settings'`); bundle→profile→home (`$DSH_HOME/cordis.patch.yml`)→`--patch` layering via `applyEntryPatches`; `!!js` expressions evaluated at activation; `settingsNamespace()` brands keys; `SettingsApplies 'live'|'restart'`; `redactSecrets` hides `role('secret')`; `$DSH_HOME/settings.yaml` hot-reloaded; `docs/config-catalog.md` is a **generated** artifact (`scripts/gen-config-catalog.ts`, cross-checked by `pnpm run verify-config-catalog`) | Layered runtime configuration; user-plane settings over composition | Config state | Boot-time | Stable | DSH-side plugin config (endpoint, freshness, budgets) rides standard layering; no parallel config system |
| S36 | Jobs / schedule / skills | `packages/jobs/jobs/src/index.ts` + `types.ts`; `packages/schedule/schedule/src/{runtime.ts, tools.ts, types.ts}`; `packages/skill/skill/src/index.ts` | `ctx.jobs` abstract `JobRegistry extends Service` (direct instantiation throws "load an implementation"); abstract `start/list/get/read/kill/wait/onJobDone/onJobsChanged/attachController`; `JobKindMap {bash:'bash', subagent:'subagent'}` (merge-extensible); `JobStatus 'running'|'stopping'|'completed'|'killed'|'failed'`; `JobStart {kind, label, outputLimitBytes?, owner?, run(): JobHooks}`; tools `job_output` (wait default 30s)/`job_list`/`job_kill`; `schedule` plugin (inject `['agents','sessions','tools','sessionPersistence']`) installs `ScheduleRuntime` per root agent via `agent/created`, drives on agent idle + `schedule/change`, delivers via `agent.followup`; tools `schedule_create/list/delete`; records `AtScheduleRecord`/`AfterScheduleRecord`/`EveryScheduleRecord`; `ctx.skills` = `SkillRegistry extends Service` (`register`/`list`/`snapshot`), `SkillProvider {name, list()}`, `BUNDLED_SKILL_RANK = 600`, SkillSource `'project-dsh'|'project-agents'|'runtime'|'user-dsh'|'user-agents'|'custom'|'bundled'`, model-facing tool `skill` + durable `skill-catalog` records | Process-local background jobs; schedule delivery via agent.followup; layered skill catalog | Job state process-local | Service lifecycle | Stable | Background DriverHost-pull jobs (if ever needed) ride jobs; skills could host UniClaw operation playbooks — later phase |
| S37 | Scope system | `packages/core/scope/src/index.ts` | `Scoped<T>` routing-only event receiver; `scopeTarget`; scope parent chain; `AnonymousEntries/NamedEntries/ScopedLayers` | Registration views inherit DOWN the chain; agent-scoped events route to one agent | Live routing | Fiber-scoped | Stable | Plugin events scoped to one run's agent (per-run live events); prevents cross-run leakage |
| S38 | Presentation vocabulary | `packages/core/tools/src/presentation.ts` | render-intent types: command card, file diff card, raw content; `presentationMeta` | Tool result render intent for UI | — | — | Stable | Evidence/trace rendering intent for uniclaw tools (structured evidence first, raw lazy) |
| S39 | User questions service | `packages/interaction/user-questions/src/index.ts` (`ctx.userQuestions.ask` ~92), `types.ts` (`AskUserQuestionItem` 35, `AskUserQuestionAnswer` 63, `AskUserQuestionIntent` 23); bridge `packages/interaction/tool-ask-user/src/index.ts` (tool `ask_user_question` via `defineTool` → `ctx.userQuestions.ask({questions, agent: exec.agent, signal})` → `{answers}`) | Single `UserQuestionProvider.ask`; validates **exact live root agent** (registry identity + `roots()`) and intent/approve-label consistency; model-facing bridge tool `ask_user_question` | Ask the human a structured question from a tool or command; answers NOT logged (no durable record by default) | Live; answers not durable by default | Per ask | Stable | Native human-in-the-loop seam: trap inspection / recovery consent / plan review; buyer-gated if durable record needed (e.g. audit) |
| S40 | Hook protocol (Claude Code / Codex bridges) | `packages/hooks/hook-protocol/src/types.ts` (`hook/invoked` 19, `hook/result` 31 — **log-only** session events, not SurfaceEventType; `HookOutput.updatedInput` parsed but **NOT honored**); `packages/hooks/hooks-claude-code/src/index.ts` (SessionStart→`agent/session-start` 206, UserPromptSubmit→`agent/pre-step` 219, PreToolUse→`tools/pre-execute` 238, PostToolUse→`tools/post-execute` 247, Stop→`agent/turn-stopping` 270, SubagentStart/Stop→`subagent/start|end` 281/291; deny→reject/block, ask→ask, context via `agent.inject`/`steer`); `packages/hooks/hooks-codex/src/index.ts` (**blocks only — allow/ask NOT honored**, comment 224; `stop_hook_active` always false 261) | External editor/agent hook protocols bridged onto DSH-native extension points; log-only observability events | `hook/invoked`+`hook/result` durable (log-only); bridge decisions live | Per hook run; config read once per process | Stable | NOT a control-plane carrier (external automation inbound, like S21/S22); documents that a hook-shaped surface degrades asymmetrically (CC honors allow/ask, Codex blocks only) — do not build a Kernel control path on hooks |
| S41 | Plan mode (logged state precedent) | `packages/plan/plan-mode/src/index.ts` | `/plan` command; durable `plan/mode` events (~440/456); `exit_plan_mode` tool stays registered while inactive; `PlanModeController`; `foldPlanMode` folds the last `plan/mode` event (~129), projection unit `plan {active, pending}`; **no live mirror** (comment ~182: "UIs observe committed flips through session/event; there is no live mirror") | Plan = logged state folded from the durable log; UI observes committed flips, not a live mirror | `plan/mode` durable | Per session | Stable | Precedent for run-state UI discipline (F13): Kernel-fact-derived UI reads committed session events/projections, never a second live truth; review ask rides userQuestions with intent `{kind:'plan-review', approve:'Approve'}` |
| S42 | Dynamic plugins (`@pluginId` seam) | `packages/extensions/tool-cordis/src/index.ts`, `cordis-host-runner/src/index.ts` | `CordisDynamicPluginId`, `CordisDynamicPackageId`, `CordisDynamicPluginRunId`; inspect providers; host-managed runs/approvals for runtime-defined plugins | Runtime-defined plugins are a separate seam from composition rows; immutable packages, version pointers, approval grants | Plugin run state (host-managed) | Per-run; stopped/undefine removes | rc.5; tool-facing API surface | Implementation-phase seam: dsh-plugin-uniclaw's dynamic extension surface if/when runtime definition is wanted; NOT required by this baseline (composition rows + presets suffice) |
| S43 | FS permission seam | `packages/fs/fs/src/index.ts`, `packages/fs/tool-fs/src/{sandbox.ts, write.ts}` | `ctx.fs` ('fs'); events `fs/write-intent`, `fs/edit-intent` (waterfall single-slot), `fs/observed`; `FsEscalation` (`schemaFields`, `resolvePolicy('write', args, exec)`, `mapError → FsError 'FS_SANDBOX_DENIED'` with denial+hint markers); escalation fields advertised only under a confining backend | Tools negotiate sandbox policy before any file mutation; DSH-internal file writes go through their own policy gate | Live | Per call | Stable | F5-adjacent evidence: even DSH file mutation rides a policy gate — physical device dispatch stays Kernel-exclusive; plugin config/log writes ride `ctx.fs` policy, never bypass |

## Docs-vs-Source Discrepancies Recorded (pinned baseline)

| # | Claim | Source Truth (pinned commit) | Impact |
|---|-------|------------------------------|--------|
| D1 | docs primer lists 4 event dispatch modes | Source `DispatchMode` has 5: `emit`, `parallel`, `serial`, `bail`, `waterfall` (`vendor/cordis/src/events.ts:32`) | Minor; `bail` is available for veto patterns; design may use `waterfall`/`serial` only |
| D2 | Function-plugin shape described with `apply` | `apply` belongs to `Plugin.Object`; `Plugin.Function` is a plain callable `(ctx, config)` (`vendor/cordis/src/registry.ts:92–133`) | Minor; dsh-plugin-uniclaw should use `Plugin.Object` with `apply(ctx, config)` |
| D3 | `docs/architecture.md:58` "validation, continuation" event labels | No verbatim event names in source; closest real events are `agent/pre-step` and `agent/request-error` / `agent/turn-stopping` | Minor; do not design against the doc labels |
| D4 | `session/flush` doc anchor at declaration | Implementation at line ~1022 (`packages/core/session/src/index.ts`) | Cosmetic |
| D5 | Root `docs/persistence.md` | Does not exist; persistence docs live in `docs/subsystems/persistence.md` + generated catalog | Cosmetic |
| D6 | ui-settings `host.call` mention | Static client modules use Typert + `connection.rpc.call`; `host.call` is only the dynamic-plugin runner seam | Minor; client-module design uses Typert Remote (S13) |
| D7 | Release stance | Pre-release: `SESSION_FORMAT_VERSION = 0`, no git tags on pinned checkout, no compatibility promise | Baseline pins the commit; forward compat is out of scope |
| D8 | Hook protocol surface appears symmetric | `HookOutput.updatedInput` is parsed but NOT honored; Codex bridge honors blocks only (`allow`/`ask` ignored, `stop_hook_active` always false) while the Claude Code bridge honors allow/ask | Do not design a Kernel control path on hooks; classify hooks as inbound automation (S40) |
| D9 | `ToolDefinition.timeoutMs` appears to be a plain per-tool field | Enforcement lives in a separate guard plugin (`timeout-policy`) listening on `tools/execute`; the registry only carries the value | Design uses `tools/execute` guard as the timeout seam, not the field |
| D10 | `docs/config-catalog.md` reads as authoritative config prose | It is a **generated** artifact (`scripts/gen-config-catalog.ts`), cross-checked by `pnpm run verify-config-catalog`; source config types (schemastery) are authoritative | Config decisions cite source types, not the catalog |
| D11 | `vendor/README.md` version table appears to mismatch vendored packages | README pins upstream snapshot versions (cordis 4.0.0-rc.7, loader 1.0.0-rc.5, schemastery 3.18.0, include 1.0.4) while vendored package.json report 4.0.1 / 1.0.2 / 3.18.1 / 1.0.6 — intentional (directory names + upstream versions deliberately unchanged) | Not a defect; baseline records vendored fork reality (locally modified Cordis) |

## Baseline Integrity Notes

- **SOURCE AUTHORITATIVE**: every row above was read from the pinned checkout, not from latest docs or generic Cordis behavior.
- **READ-ONLY**: no DSH file was modified during this audit; the pinned checkout remains pristine.
- **Falsifier linkage**: S7 (commands) is the F8/F11 evidence for deterministic human control without model turns; S39 (user questions) extends F8 evidence (human-in-the-loop without durable log unless buyer-gated); S17 (projection) is the F13 evidence that DSH UI state stays projection-derived, never Kernel truth; S41 (plan mode "no live mirror") is a second F13 precedent; S1/S2/S3 are the F4 evidence that a plugin-declared session-event vocabulary IS a DSH-native seam (no parallel protocol invented); S9/S10/S34 are the F5/F6/F7 evidence that physical dispatch, GoalEvidence creation, and second-state-ownership stay Kernel-exclusive; S43 extends F5 evidence (DSH file mutation rides its own policy gate; device dispatch stays Kernel-exclusive); S40 documents why hooks are NOT a control-plane carrier (D8); S42 records the dynamic-plugin seam for the implementation phase (not needed by this baseline).
- **Structural finding (session-event fan-out)**: session events are NOT Cordis events — the log reaches listeners through the single `session/event` emit; persistence and telemetry subscribe to it in-process, the SDK server re-emits it over JSON-RPC (`session.event`), and the apiproxy pushes it over the mux WebSocket. Three delivery paths from one in-process source → the observability mapping needs NO new emitter: a plugin-appended Kernel-fact event automatically reaches durable persistence (S19), live listeners (S5), and the browser (S16) through existing seams.
