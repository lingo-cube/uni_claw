# SourceEvidenceMatrix — dsh-shadow-cognition

> Change: `dsh-shadow-cognition`
> Pinned DSH baseline: `47f943859bef60e4160492346772ded9b24f765a`
> DSH version: `0.1.0-rc.5`
> Audit method: READ-ONLY. Every row verified against the pinned checkout at
> `/Users/fran/Documents/Code/dk-harness` (commit `47f943859b`). SOURCE IS
> AUTHORITATIVE; line numbers reference the pinned checkout and may drift on
> later commits — the File + Symbol columns are the stable identity.
> Row count: 20 rows. M1–M12 = seam matrix (M1–M10 chosen by the baseline at
> creation; M11–M12 audited and NOT selected). **REBASELINE 2026-08-15**
> (`PROJECT_LEADER_DSH_SHADOW_COGNITION_DURABILITY_REBASELINE_DECISION`,
> `BASELINE_ASSUMPTION_FALSIFIED`): V1 durability is `EPHEMERAL_PROCESS_LOCAL`
> with ZERO custom session events. **Selected-for-V1 now: M1, M7 (M11 stays
> precedent-only). NOT-selected-for-V1: M2–M6, M8–M10** — the seams audited
> for the `DSH_NATIVE_DURABLE` path, falsified by M13–M20 below. M12 remains
> NOT selected. The falsification is NOT an implementation/Kernel/DriverHost/
> plugin defect.

## Matrix (M1–M12)

| # | DSH Concern | Source File (pinned checkout) | Symbol | Behavior | Why selected | Compatibility constraints |
|---|-------------|-------------------------------|--------|----------|--------------|--------------------------|
| M1 | Model invocation seam | `packages/llm/llm/src/index.ts`; `packages/llm/llm/src/types.ts` | `LlmRuntime` (class, ~284), `ctx.llm`; `stream(options: GenerateOptions)` (~171 abstract / ~913 impl); `llm/stream` waterfall (~911); `listProviders` (~419); `GenerateOptions` (types.ts:320: `provider`, `model`, `messages`, `system`, `tools`, `temperature`, `maxTokens`, `stop`, `signal`, `sessionId`, `purpose?`); `purpose` closed enum `'compaction'\|'session-title'` (types.ts:355) | The pinned provider-neutral LLM service: adapter registry + streaming model call, interceptable via `llm/stream`. Caller supplies provider/model; adapters map to provider wire format | **SELECTED for V1** — THE DSH-native model invocation seam (D6). Shadow builds one one-shot `GenerateOptions` and calls `ctx.llm.stream`; provider/model come from DSH config; no new provider framework | `purpose` is a closed enum — Shadow leaves it unset (only `compaction`/`session-title` are admitted; ordinary conversation requests leave it unset). `markAgentLoopRequest` (call-config.ts:66) is loop-only — Shadow does NOT use it. Output assembled from `StreamChunk` via `BlockAssembler` (index.ts:42 export) |
| M2 | Agent session access | `packages/core/agent/src/runtime-types.ts` | `readonly session: Session` (~70) | The Agent object exposes its owned DSH `Session` | **NOT selected for V1 durability path** — the command handler receives `agent` (`CommandInvocation.agent`), so Shadow can read `agent.session` for session identity only; V1 appends NOTHING to it (no durable event, §9) | Session object is Host-internal live data; if a future durable iteration ever appends, it must use the public `Session.append` API only, never mutate the log |
| M3 | Session append (event log write) | `packages/core/session/src/index.ts` | `Session.append<T>(type, data, ...opts)` (~606); `assertSupportedRequestHeader` (~363); `Session.events` / `seq` | Validated append-only write: lossless-JSON snapshot, `SurfaceManager.validateNext`, deep-freeze, `seq = log.length`, post-commit `session/event` fanout with reentry guard. No known-set membership check at append; `assertSupportedRequestHeader` rejects only legacy `request/header-delta` and `request/header reason=fallback` | **NOT selected for V1** — audited as the durability writer path (the commands registry uses it for in-repo `command/run`/`command/done`), but a live append of the out-of-repo type `shadow/analysis` persists NON-ignorable (M13/M14) and makes the whole session log refuse to reload (M18). Shadow V1 writes ZERO custom session events | Runtime JS accepts any string `type`; the persistence READ path enforces known-set + ignorable (M5) — but live `Session.append` cannot set the marker (M14), so "reload-safe via live append" is not implementable |
| M4 | Session event envelope + ignorable vocabulary growth | `packages/core/session/src/types.ts`; `packages/core/session/src/known-event-types.ts` | `SessionEvent` envelope (~404): `{ type, seq, time, data, ignorable? }`; `ignorable?: true` (~422: absent = required; unrecognized required event → reader MUST refuse); `KNOWN_SESSION_EVENT_TYPES` (generated, 44 members; out-of-repo plugin types excluded by construction); `SESSION_FORMAT_VERSION = 0` (~56) | The durable per-session event vocabulary; the envelope's `ignorable` marker is the documented vocabulary-growth path: "Adding an ordinary event type does not bump — the per-event `SessionEvent.ignorable` guard covers vocabulary growth instead" | **NOT selected for V1** — the marker is real in the envelope type and the READER honors it, but there is no supported live-writer path for an out-of-repo plugin to set it (M13/M14/M16). V1 appends no `shadow/analysis` event at all, so no marker is needed | Marker present only on seed/restore + coordinator-append envelopes, never via live `Session.append`. `SESSION_FORMAT_VERSION` stays 0 |
| M5 | Persistence read-path enforcement | `packages/session/session-persistence/src/coordinator.ts` | `PersistenceCoordinator` (~588); `assertEventsSupported` (~1063): `if (KNOWN_SESSION_EVENT_TYPES.has(event.type) \|\| event.ignorable === true) continue` else throw `SessionFormatUnsupportedError` | On load, refuses a log containing an unknown non-ignorable type; skips unknown ignorable ones | **NOT selected for V1** — the rule is confirmed by probe (M18/M19): reload-safe only when `ignorable: true`, which live append cannot produce; safe by construction in V1 because zero custom events are appended | Falsifier SHADOW-F15 rebaselined: reload-safety is now guaranteed by "no custom event exists", not by a marker |
| M6 | Persistence write path (native durability) | `packages/session/session-persistence/src/coordinator.ts`; `packages/session/session-persistence-sqlite/src/index.ts` | write path `session/event` → flush drain (~1084, ~1123); `session/flush` awaited-parallel checkpoint (~1129); SQLite backend persists `ignorable` column (index.ts:34–42) | Existing backends persist every committed session event (write-behind batching + awaited flush barrier) | **NOT selected for V1** — native write-behind persists only what the live session itself appended (M17); it does not create a sanctioned external-injection path, and the filter can silently drop events on seq misalignment (M19 Path C) | Persistence failure is contained (observer failures logged, append already committed) — fail-open per D8; irrelevant in V1 (no Shadow append) |
| M7 | Commands registry (human surface) | `packages/interaction/commands/src/index.ts` | `CommandRuntime` (~225); `CommandInvocation { commandId, agent, rawInput, signal }` (~28–34); `register` (~241/245); `list` (~260); `find` (~273); `execute` returns `{ commandId, result }` (~343); lifecycle `command/run`+`command/done` appended to `agent.session` (appendLifecycle ~350) | Plugin-owned slash-command registry; handler runs against the receiving agent WITHOUT sending the command to the model (zero-model, deterministic) | **SELECTED for V1** — `uniclaw-shadow-analyze` mounts here — the DSH-native human inspection surface (D7), same convention as the graduated `uniclaw-*` commands; its response is the authoritative inspection surface (§11) | Session-scoped by construction; naming must follow the `uniclaw-` kebab-case convention; command dispatch stays zero-model; lifecycle events are in-repo known types |
| M8 | Live session fanout | `packages/core/session/src/index.ts` | `session/created` (~47), `session/event` (~73), `session/flush` (~85, impl ~1022) — all emit-mode, scope-filtered, contained observers | Every committed session event is broadcast live (persistence, telemetry, mux); no veto | **NOT selected for V1** — fanout covers only live-committed events; in V1 no Shadow event exists to fan out. The drain is still used read-only to serve human requests | Unknown non-surface types are ignored by projections (M9) and by the UI; irrelevant in V1 (no Shadow event) |
| M9 | Session projection registry | `packages/session/session-projection/src/index.ts` | `SessionProjectionRegistry` (`ctx.sessionProjections`, ~171); subscribes `session/event` (~179); `register(definition)` (~194) | Pure fold over committed session events into validated wire payloads; drives registered units only | **NOT selected for V1** — audited: an unknown `shadow/analysis` type matches no projection unit (no-op), but V1 appends no Shadow event so there is nothing to project | If a later iteration wants a live Shadow read model, it registers a projection unit (out of scope for the baseline) |
| M10 | Session store + typert lookup | `packages/core/session/src/index.ts` | `SessionStore` (`ctx.sessions`, ~792); `typert.lookups.register('session', …)` (~805–813) | In-memory session registry; typert 'session' lookup for RPC consumers | **NOT selected for V1** — session identity is still read for the artifact's `sessionId` field (D9), but no durable event carries it | Live-data object — plugins must read leaf fields only |
| M11 | Agent-loop model call precedent | `packages/core/agent-loop/src/agent.ts`; `packages/llm/llm/src/call-config.ts` | `markAgentLoopRequest(deepFreeze({...GenerateOptions}))` (agent.ts:486); `markAgentLoopRequest` / `isAgentLoopRequest` (call-config.ts:66/76) | The agent loop builds a `GenerateOptions` with derived history and stamps it as a loop request before calling the LLM seam | Precedent only — confirms `ctx.llm` is THE model seam and that a one-shot, non-loop call is a plain `GenerateOptions` without the loop marker (D6) | Shadow must NOT mark its call as a loop request; `purpose` unset; no derived-history assembly |
| M12 | Model-facing tools registry | `packages/core/tools/src/index.ts` | `ctx.tools` (`ToolRegistry extends Service`, ~827); `register(definition)` (~1037) | Model-facing tool registry with JSON-schema parameters, guard waterfalls, timeout policy | NOT selected — audited: Shadow does deterministic retrieval before the call and exposes NO tools to the model (§19, "Prefer NO model-facing Kernel mutation tools") | If a later iteration adds read-only retrieval tools (get snapshot / bounded events / resolve EvidenceRef), it must use this registry with read-only methods only — no mutation surface |

## Falsification evidence (M13–M20)

Evidence for the durability rebaseline
(`BASELINE_ASSUMPTION_FALSIFIED`, 2026-08-15). Three kinds, clearly
distinguished: **SOURCE FACT** (verified against pinned source), **PROBE
RESULT** (observed from a real pinned boot with cold reload — probes
`/tmp/dsh-shadow-probe2.mjs`, two-boot design: write phase ctx1 → dispose →
read phase ctx2 over the same storage root), **PROJECT LEADER DECISION**
(frozen decision of this gate).

| # | Kind | Evidence | Detail (pinned checkout / probe) | Impact on baseline |
|---|------|----------|----------------------------------|--------------------|
| M13 | SOURCE FACT | `Session.append` envelope construction has NO ignorable path for callers | `Session.append` builds `deepFreeze({ type, seq, time, data: dataSnapshot, ...surfaceMetadata })`; `surfaceMetadata` comes ONLY from `opts[0]`'s `SurfaceIntent` (`sourceEventSeqs`/`surfaceOp`) — `packages/core/session/src/index.ts:604–655`. There is no supported way for an out-of-repo caller to set `ignorable: true` on a live append | The frozen baseline assumed `Session.append('shadow/analysis', …)` could carry the marker; it cannot. A live-appended unknown event persists non-ignorable |
| M14 | SOURCE FACT | `assertSessionEventEnvelope` accepts `ignorable` only as `true`, and only on seed/restore + coordinator-append envelopes | `packages/core/session/src/index.ts:213–250`: envelope validation accepts the `ignorable` key only with value `true`; it appears on seed/restore and coordinator-appended envelopes, never on envelopes produced by live `Session.append` | Confirms M13: the marker is a real envelope field but not reachable from the live append path a plugin would use |
| M15 | SOURCE FACT | `KNOWN_SESSION_EVENT_TYPES` excludes out-of-repo plugin types by construction; registration surface deferred | `packages/core/session/src/known-event-types.ts` (generated by `scripts/gen-persistence-catalog.ts`, 44 members): doc states out-of-repo plugin event types are excluded by construction and "a registration surface for them is deferred until such a consumer exists" | There is NO supported runtime registration surface for an external `shadow/analysis` type today (deferral condition A of §9.4 is not met) |
| M16 | SOURCE FACT | `PersistenceCoordinator.append` can inject raw ignorable envelopes but is un-sanctioned for plugins | `packages/session/session-persistence/src/coordinator.ts`: public awaited `append` (~669) accepts raw envelopes including ignorable ones; `assertEventsSupported` (~1063) enforces known-set-or-ignorable on read; contiguity enforced (seq must equal stored cursor). ZERO in-repo callers outside coordinator-contract tests, which exercise only the DETACHED `create`+`append` pattern (`tests/coordinator-contract.ts:709–724`, `plugin/test` ignorable event) | Direct coordinator append bypasses the authorized live append/fanout semantics; the only in-repo precedent is a detached-session test, not a live-session plugin writer |
| M17 | SOURCE FACT | Write-behind filter can silently drop live events on seq misalignment | `appendLiveBatch` (~1063) does `batch.filter(e => e.seq >= cursor)`; the in-code comment ties the filter to "events initialization already stored" (seed prefix), NOT external injection; write path `installWritePath` (:1084–1141) enqueues only events the live session itself appended via `session/event` | A direct-persistence event with `seq` below the stored cursor causes the NEXT live event to be silently dropped from storage (proven in M19 Path C) |
| M18 | PROBE RESULT | Path A: live `session.append('shadow/analysis', …)` + flush → cold reload REFUSES the whole log | Real pinned boot (`boot()` from `packages/boot/app-boot/lib/index.js`, absolute-path composition rows, jsonl backend, temp root; second boot = cold reload): event persisted with `ignorable: undefined` → `SessionFormatUnsupportedError: session "probe-a" contains event type "shadow/analysis" (seq 0) unknown to this harness and not marked ignorable; refusing to interpret the log` | FALSIFIES the frozen "required chain" (Session append → fanout → coordinator → flush → reload-safe). V1 cannot use this path |
| M19 | PROBE RESULT | Path B/C: direct coordinator append works only with unsafe seq coupling; misalignment silently loses events | Path B (flush → `persistence.append(id, [shadow@seq=liveLen, ignorable:true])` → live shadow append → live command/done): cold ACCEPTED `0:command/run | 1:shadow/analysis[ignorable] | 2:command/done` — but reproduces the un-sanctioned dual-write with exact live/persistent seq parity. Path C (same minus the live shadow append): cold ACCEPTED but `command/done` SILENTLY DROPPED from storage (`0:command/run | 1:shadow/analysis[ignorable]`) | Any production reproduction of Path B/C depends on private coordinator behavior and demonstrated silent event loss — FORBIDDEN as a workaround (§9.3) |
| M20 | PROJECT LEADER DECISION | Rebaseline frozen: V1 durability = `EPHEMERAL_PROCESS_LOCAL`, zero custom session events | Shadow V1 appends ZERO custom session events (`DurableEventType = NONE`; no `shadow/analysis` event exists; no `ignorable` requirement — nothing is appended). Completed analyses MAY live in a bounded process-local cache (`Map<runId, bounded recent ShadowAnalysis>`, non-authoritative, disposable — NOT a Memory/Knowledge Store/History Database). DSH restart → cache empty → Kernel unaffected → fresh analysis recomputable from current legitimate read surfaces; lost analyses NEVER reconstructed/fabricated. **DUAL-WRITE WORKAROUND: FORBIDDEN. DETACHED SHADOW SESSION: FORBIDDEN.** Deferred pressure item `DSH_SHADOW_DURABILITY_EXTENSION` recorded (design.md §9.4), reconsiderable only when a condition A–D exists. Buyer (post-hoc / near-live run interpretation) still satisfied | This gate's source of truth for V1 durability; falsifiers SHADOW-F7/F15 rebaselined to truthful-loss semantics (design.md §16) |

## Implementation evidence (M21–M28)

Evidence produced by the implementation gate
(`PROJECT_LEADER_APPLY_DSH_SHADOW_COGNITION_V2`, 2026-08-15). Each falsifier
F1–F16 maps to a deterministic test (unit falsifier suite
`dsh-plugin-uniclaw/test/shadow.test.mjs`, real pinned-DSH integration
`test/real-host-shadow.test.mjs`, architecture guards
`test/architecture-guards.test.mjs`). Real-host tests boot the REAL pinned DSH
host (commit `47f943859bef60e4160492346772ded9b24f765a`, cordis 4.0.1) with the
REAL dsh-commands registry, the REAL LlmRuntime (adapter registered via
`registerAdapter(['test-shadow'], …)`), a REAL detached `Session`, and a
wire-conformant loopback DriverHost fixture that records every requested RPC
method.

| # | Kind | Falsifier(s) | Evidence | Where proven |
|---|------|--------------|----------|--------------|
| M21 | TEST RESULT | F1, F2, F13, F14 | Trap focus analysis carries the bounded trap detail + the TrapRaised EventId as a `kernel-fact`; `RunFailed` cause is surfaced as `shadow-inference`, never as a Kernel fact; `sessionId` = truthful DSH session id (`session-shadow-real`), never equal to `runId`; epistemic labels explicit in the command response (`[kernel-fact]` / `[derived-read-model]` / `[shadow-inference]` / `[human-investigation]`) | `shadow.test.mjs` trap-focus + identity tests; `real-host-shadow.test.mjs` ShadowCommandEndToEnd |
| M22 | TEST RESULT | F3, F4 | Missing snapshot fields → `missing-data` uncertainties; unresolvable EvidenceRef → `unresolved-evidence-ref` uncertainty with the analysis still produced and usable (fail-open retrieval, fail-closed model parse) | `shadow.test.mjs` missing-data + unresolved-ref falsifiers |
| M23 | TEST RESULT | F5, F6, F7 | Real 50ms timeout → `model-timeout` (ref'd timer + `AbortSignal.any`, deterministic in a minimal event loop); model throw → `model-error`; restart (fresh boot in the same process AND full ctx dispose) → cache empty, fresh analysis, id sequence continues only as a process-local counter — no reconstruction, no fake history | `shadow.test.mjs` timeout/error falsifiers; `real-host-shadow.test.mjs` ModelUnavailableAfterDispose + RestartIsTruthfulLoss |
| M24 | TEST RESULT | F8 | Real pinned host: the fixture records every wire method requested by the shadow path; after the whole scenario the requested set is a subset of the frozen six read-only methods (`ping, run.list, run.snapshot.get, run.events.after, run.trap.get, evidence.get`) — zero run.pause/stop/abort, zero action.\*, zero adb.\*, zero run.events.drain, zero control.support | `real-host-shadow.test.mjs` F8WireProof |
| M25 | TEST RESULT | F9, F10, F16 | Zero GoalEvidence/Container/Binding/StateBelief creation language in plugin source (only the frozen "Kernel GoalEvidence indicates …" epistemic phrase); zero session writes (`session.append`/`Session.create`/`new Session`/`.flush()` absent from src/**); adapter `_request` literals = exactly the frozen 8 wire methods; modified files under `src/UniClaw.Runtime` = exactly the pre-existing Phase 0-3 baseline (zero new, zero untracked, zero shadow content in runtime diffs) | `architecture-guards.test.mjs` (3 guards); `real-host-shadow.test.mjs` session-events assertions (only `command/run` + `command/done`) |
| M26 | TEST RESULT | F11, F12 | Overflow context deterministically trimmed (drop oldest non-priority events, then hard-slice; never exceeds `maxContextChars`); `shadow.visual.enabled` defaults false → zero visual inputs; MaxEvents/MaxEvidenceRefs/evidenceBytesPerRef caps enforced in assembly and in the artifact schema | `shadow.test.mjs` boundedness + F11/F12 falsifiers |
| M27 | TEST RESULT | F15 | Real pinned host: after two shadow invocations the real `Session.events` view contains ONLY `command/run` + `command/done` (4 events) — zero custom shadow events, zero persistence writes; full ctx dispose + fresh boot proves restart-loss truthfulness | `real-host-shadow.test.mjs` ShadowCommandEndToEnd + SecondInvocation + RestartIsTruthfulLoss |
| M28 | TEST RESULT | Model seam, §10/§21 | One real `ctx.llm.stream()` call per success analysis (`ModelCalls = 1`), provider/model forwarded to the adapter, bounded user message contains the read-model + kernel-fact context, no loop marker, no tools, `purpose` unset; after `handle()` (adapter route release) → `NO_ADAPTER` finish chunk → status `not-configured` + `model-unavailable`, zero model calls, deterministic digest still returned | `real-host-shadow.test.mjs` ShadowCommandEndToEnd + ModelUnavailableAfterDispose; `shadow.test.mjs` model seam falsifiers |
