# STOPPED_AT_NEW_RUNTIME_OR_CONTRACT_GAP — STOP-3: STOP-2 mechanism erratum + real gap relocation

Per Implementation Gate §12 ("STOP-2 RED 无法稳定复现" branch), after WI-VEC-0's
evidence-first derivation. **I.1–I.8 are NOT entered.**

## Expected Reality (at gate open)

The authorized change's premise (STOP-2): the frozen extension-only normalization
contract cannot classify a stable zero-new-source exhaustion-confirmation window, so a
deterministic RED (old contract Unresolved → new contract Resolved) exists in the
archived run-6 evidence.

## Observed Reality (empirically reconstructed, public surface only)

- The **canonical STOP-2 shape resolves on TODAY's code** (frozen test
  `CanonicalStop2Shape_TerminalPair_ResolvesOnOldContract_ErratumConfirmed`: the
  identical terminal pair IS the unique union-tail suffix). The recorded mechanism
  ("union-overlap absent/ambiguous on the terminal confirmation pair") **does not exist
  in the evidence** — it was a leader diagnostic error, now locked as an erratum.
- Run-6's ACTUAL Unresolved: frames 13/14 render one logical row twice as corroborated
  `text_block` occurrences (`Sound & vibration|text_block` ×2 — stacked title + OCR dup
  at the caption offset, vertically adjacent so the same-LINE dedup correctly does not
  merge them), BOTH admitted via structured-row corroboration → in-frame duplicate
  signature → old-contract fail-closed (`Observation 14 contains duplicate structured
  navigation signatures`).
- **Formal obstruction**: the spec'd confirmation class ⊆ the old-resolved class —
  no input exists where old=Unresolved ∧ new=Resolved. The mandated I.5
  RED→GREEN is unconstructible from the archived evidence.

## Reality Gap

The authorized capability (`VIEWPORT_EXHAUSTION_CONFIRMATION`) targets a contract gap
that the evidence refutes; the campaign's true remaining blocker is elsewhere.

## Evidence Reference

- `tests/UniClaw.Runtime.Tests/Unit/ViewportExhaustionConfirmationRedTests.cs` (3
  frozen tests, leader-verified 3/3: mechanism-lock RED on the REAL sequence, extending
  control, erratum control);
- `evidence/semantic-ir-records.md` (erratum recorded at gate open);
- archived `../runtime-iterative-full-traversal-acceptance/evidence/G-stage-a/reentry/run6-frames.json`;
- run-5 frame-9 geometry (stacked `Sound & vibration` title + dup at caption offset).

## First Divergence Point

One structured row corroborating MULTIPLE same-text vision occurrences (stacked title +
caption-offset OCR duplicate) → two admitted occurrences with identical signatures in
one frame.

## Owner

- The exhaustion-confirmation contract gap: **NONEXISTENT** (erratum; withdrawn).
- The real gap spans: perception fusion dedup (same-LINE rule does not cover STACKED
  same-text duplicates — by design) × `SettingsSemanticCapability` corroboration
  (admits every matching occurrence, not a unique best match). Both are OUTSIDE the
  current authorization (perception batch complete; capability production-frozen).

## Missing Capability

Unique corroboration admission: one structured row may corroborate at most ONE vision
occurrence per frame (deterministic best-match), OR the fusion dedup extends to
stacked same-text pairs within one band. Either requires a NEW Human-authorized change
(this one's scope cannot cover it).

## Authority Impact

NONE — zero production code changed in this attempt (tests only, all green; the frozen
suite honestly locks the erratum and the real mechanism).

## Disposition of this change

Recommend: **withdraw or re-scope** `runtime-viewport-exhaustion-confirmation`
(premise refuted). A follow-up change for the REAL gap (unique-corroboration admission)
would be small, evidence-backed (the frozen RED already pins the failing shape), and
directly unblocks the reentry campaign — but it needs fresh Human authorization.

## Leader's error acknowledgment

STOP-2's mechanism was my inference, not a proven reproduction. The cost: one authorized
change built on a wrong premise, caught before any production edit by the ruling's own
RED-first discipline — which is exactly what that discipline is for.
