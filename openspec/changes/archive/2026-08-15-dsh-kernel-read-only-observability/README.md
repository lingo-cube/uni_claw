# dsh-kernel-read-only-observability

Read-only kernel observability projection (RuntimeEvent + RunSnapshot + EvidenceRef) for the future DriverHost / DSH control plane. Observability only: no cognition, no Shadow/Advisory/Blocking, no control-plane physical actions.
