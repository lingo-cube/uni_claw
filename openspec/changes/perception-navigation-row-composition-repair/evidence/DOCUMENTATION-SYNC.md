# DOCUMENTATION_SYNC

## Canonical Source / Contract

- `NO_CHANGE` for Runtime Architecture Contract, Runtime charter, Agent/FSM/Traversal contracts, and Phase 2 graduation sources.
- Reason: provider-side composition and narrow downstream eligibility filters enforce the already-frozen Vision-primary boundary. Runtime exact overlap and fail-closed semantics remain unchanged; auxiliary hierarchy evidence does not gain authority.
- The new behavior contract is owned by this active OpenSpec specification.

## Relevant Layer / Pattern Docs

- `NO_CHANGE` for `docs/architecture/vision.md` and Runtime layer/pattern docs.
- Reason: Vision remains a subordinate evidence-producing capability; no ownership, public API, or architecture pattern changes. The retained Runtime filter is an authority enforcement detail, not a new contract.

## Current Projections

- `UPDATE`: `docs/work/active/current-gates.md` and `docs/snapshots/latest.md` active membership/counts.
- Authority: `NONE`.
- Reason: creating the OpenSpec changed the mechanically derived active-change source inventory from 18 to 19. The projection records the existing Vision deployment promotion gate without granting it.

## Decision / Archive Receipt

- `DEFER_TO_ARCHIVE`.
- Reason: no graduation, archive, promotion, or final lifecycle decision has occurred.

## Main Spec Sync

- `DEFER_TO_ARCHIVE`.
- Reason: the new capability delta remains under the active change until authorized archive.
