# 多源观察采用并行常开 + 分类终审，不做选路与数值合并

多源（视觉 / XML 树 / 深模型）观察**永远并行、全页面启用**，不按域
选路：源的缺席本身是数据（degraded:no-xml 记入 provenance）。分歧
不用数值置信度加权或投票解决，而由统一裁决管道按**类别权威**分层
定案（标准控件 XML 终审 → 裁剪重扫 → 深模型，见 ADR-0028 与
PER-009）。拒绝的备选：域门控选路（三方 app 质量未知就先不接——
被否，因为缺席可表达、统计可校准）；数值置信度合并（跨源不可比、
对抗可刷分、阈值即隐藏政策）。
