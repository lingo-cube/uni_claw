# 信任等级挂（源×类别）组合、CSS 级联覆盖，不挂单条证据

producer 的采信度以 `(源 × claim 类别) → A/B/C` 等级表表达
（producer-trust.json，随程序集版本化），查找按 CSS 级联：包名/
系统特例 > 类别规则 > 源默认；修订人审 change 与自动调优（留痕可
回滚）双通道。等级挂组合、不挂单条证据记录：每级对应一个可审计
的采信动作（A=孤证可授权常规动作；B=不可逆前补佐证；C=只算线索），
凡不能改变决策的数值精度均不引入。拒绝的备选：逐条证据挂数值分
（可刷分、诱导加权平均）；全局源排名（可信度随上下文双向翻转，
非支配关系不存在全序）。
